// 표준 내장 객체
// 자바스크립트에서 기본으로 제공해주는 객체 mdn 사이트에 잘 정리 되어 있음
// 유용하게 사용할 수 있는 몇가지! string , array , data , math

//string 객체
/*const str = 'abc';
console.log(str.length); // length 문자열의 길이를 알 수 있음

const email = 'testnaver.com';
if(email.includes('@') === false){
  console.log('@가 빠져있습니다');
} // includes 해당 문자가 포함되어 있는지?

//array 객체
const arr = ['a' , 'b', 'c'];
//파괴적 메서드 비파괴적 메서드  원본배열이 손상되면 전자 , 아니면 후자
arr.push('d'); // pop 뺴는것 push 더하는 것
console.log(arr);

arr.forEach(function(v){  // 원본배열이 사라지지 않음
  console.log(v);
});
console.log(arr);

//date 객체
const date = new Date();
console.log(date.getFullYear());

//math  객체
const rand = Math.random() * 10 + 1;
console.log(Math.floor (rand));
if(rand < 5){
  console.log('유레카');
}*/

//웹브라우저에서 제공 하는 객체 
 